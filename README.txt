ssese is an implementation of a structural type equivalence for XML types. We consider both simple and complex types. However, the restriction type in XML schema has not been considered in this implementation.

Plans are underway to re-implement this package in ruby in order to take full advantage of the BDD testing suite.